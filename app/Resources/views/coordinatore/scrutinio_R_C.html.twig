{% extends 'coordinatore/index.html.twig' %}

{% block pagina_contenuto %}
<div class="container-fluid">
  {% include 'ruolo_docente/menu_coordinatore.html.twig' %}
  {% if not classe %}
  <div class="alert alert-warning gs-mt-4" role="alert">
    <strong class="gs-big">{{ 'message.classe_mancante'|trans }}</strong>
  </div>
  {% else %}
  <h1 class="text-center gs-h1 gs-mt-2 gs-mb-0">Ripresa dello scrutinio sospeso per la classe {{ classe.anno }}ª {{ classe.sezione }}</h1>
  <h2 class="text-center gs-h2 gs-mt-0 gs-mb-4">Scrutinio chiuso</h2>
    {% for messaggio in app.flashes('errore') %}
      {% if loop.first %}
  <div class="alert alert-danger gs-mb-4" role="alert">
      {% endif %}
    <strong>{{ messaggio|trans }}</strong><br>
      {% if loop.last %}
  </div>
      {% endif %}
    {% endfor %}
  <div id="gs-fixed-header">
    <table class="table table-bordered table-hover table-striped ">
      <caption class="text-center text-info"><strong class="gs-big">Ripresa dello scrutinio sospeso</strong></caption>
      <thead class="bg-success">
        <tr>
          <th class="col-sm-2">Documento</th>
          <th class="col-sm-7">Note</th>
          <th class="col-sm-3">Azioni</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><strong>Verbale</strong></td>
          <td>
            Stampare in duplice copia e consegnare in Segreteria Didattica: una copia sarà inserita nel registro dei verbali e l'altra conservata agli atti.<br>
            <strong><em>Controllare che non ci siano errori.</em></strong><br>
            <br>
            Formato del documento: <em>LibreOffice (compatibile con Word)</em>
          <td>
            <a class="btn btn-primary btn-xs" href="{{ path('pagelle_classe', {'classe': classe.id, 'tipo': 'V', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>Scarica</strong></a>
          </td>
        </tr>
        <tr>
          <td><strong>Riepilogo voti</strong></td>
          <td>
            Stampare in duplice copia e consegnare in Segreteria Didattica: una copia sarà allegata al verbale e l'altra conservata agli atti.<br>
            <strong><em>Controllare che non ci siano errori.</em></strong><br>
            <br>
            Formato del documento: <em>PDF</em>
          </td>
          <td>
            <a class="btn btn-primary btn-xs" href="{{ path('pagelle_classe', {'classe': classe.id, 'tipo': 'R', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>Scarica</strong></a>
          </td>
        </tr>
        <tr>
          <td><strong>Tabellone voti</strong></td>
          <td>
            Stampare in duplice copia e consegnare in Segreteria Didattica: una copia sarà pubblicata e l'altra conservata agli atti.<br>
            <strong><em>Controllare che non ci siano errori.</em></strong><br>
            <br>
            Formato del documento: <em>PDF</em>
          </td>
          <td>
            <a class="btn btn-primary btn-xs" href="{{ path('pagelle_classe', {'classe': classe.id, 'tipo': 'T', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>Scarica</strong></a>
          </td>
        </tr>
        <tr>
          <td><strong>Foglio firme Verbale</strong></td>
          <td>
            Stampare in duplice copia e consegnare in Segreteria Didattica: una copia sarà allegata al verbale e l'altra conservata agli atti.<br>
            <strong><em>Far firmare a tutti i docenti entrambe le copie.</em></strong><br>
            <br>
            Formato del documento: <em>PDF</em>
          </td>
          <td>
            <a class="btn btn-primary btn-xs" href="{{ path('pagelle_classe', {'classe': classe.id, 'tipo': 'F', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>Scarica</strong></a>
          </td>
        </tr>
        <tr>
          <td><strong>Foglio firme Registro dei voti</strong></td>
          <td>
            Stampare <em>un'unica copia</em> e consegnare in Segreteria Didattica, dove sarà conservata agli atti.<br>
            <strong><em>Far firmare a tutti i docenti.</em></strong><br>
            <br>
            Formato del documento: <em>PDF</em>
          </td>
          <td>
            <a class="btn btn-primary btn-xs" href="{{ path('pagelle_classe', {'classe': classe.id, 'tipo': 'I', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>Scarica</strong></a>
          </td>
        </tr>
    {% if classe.anno == 2 %}
        <tr>
          <td><strong>Certificazione competenze</strong></td>
          <td>
            Stampare in un'unica copia e consegnare in Segreteria Didattica, dove sarà conservata agli atti.<br>
            <strong><em>Controllare che non ci siano errori.</em></strong><br>
            <br>
            Formato del documento: <em>PDF</em>
          </td>
          <td>
            <a class="btn btn-primary btn-xs" href="{{ path('pagelle_classe', {'classe': classe.id, 'tipo': 'C', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>Scarica</strong></a>
          </td>
        </tr>
    {% endif %}
        <tr>
          <td><strong>Comunicazione Non Ammessi</strong></td>
          <td>
            Documento mostrato direttamente ai genitori sul Registro Elettronico, o stampato e consegnato dalla Segreteria Didattica nel caso di impossibilità di accesso al Registro Elettronico.<br>
            <strong><em>Controllare che non ci siano errori.</em></strong><br>
            <br>
            Formato del documento: <em>PDF</em>
          </td>
          <td>
    {% for a in dati.non_ammessi %}
            <div class="gs-mb-2"><a class="btn btn-primary btn-xs" href="{{ path('pagelle_alunno', {'classe': classe.id, 'alunno': a.id, 'tipo': 'N', 'periodo': 'R'}) }}" role="button" title="Scarica il documento"><span class="glyphicon glyphicon-download-alt gs-mr-2"></span><strong>{{ a.cognome }} {{ a.nome }}</strong></a></div>
    {% else %}
            <span class="label label-warning">NESSUNA COMUNICAZIONE</span>
    {% endfor %}
          </td>
        </tr>
      </tbody>
    </table>
  </div>
    {% if dati.precedente %}
  <div class="row text-center gs-mt-5 gs-mb-5">
    <a class="btn btn-danger gs-wait-page" href="{{ path('coordinatore_scrutinio', {'classe': classe.id, 'stato': '4'}) }}" role="button" title="Annulla la chiusura dello scrutinio e torna al passo precedente"><span class="glyphicon glyphicon-backward gs-mr-2"></span><strong>Riapri lo scrutinio</strong></a>
  </div>
    {% endif %}
    {% include 'include/modal-waiting.html.twig' %}
  {% endif %}
</div>
{% endblock %}

{% block pagina_js_fine %}
{{ parent() }}
<script src="{{ asset('vendor/floatThead/jquery.floatThead.min.js') }}"></script>
<script>
$(document).ready(function() {
  $('#gs-modal-waiting').on('shown.bs.modal', function (event) {
    $('form[name="scrutinio"]').submit();
  });
  $('a.gs-wait-page').click(function (event) {
    $('#gs-modal-waiting').modal('show');
    return true;
  });
  $('#gs-fixed-header table').on("floatThead", function(e, isFloated, floatContainer) {
    var c=floatContainer.find('thead');
    if(isFloated){
      c.hide();
      c.removeClass("bg-success").addClass("bg-info");
      c.fadeIn();
    } else {
      c.removeClass("bg-info").addClass("bg-success");
    }
  });
  // init
  $('#gs-fixed-header table').floatThead({top: 60});
});
</script>
{% endblock %}
