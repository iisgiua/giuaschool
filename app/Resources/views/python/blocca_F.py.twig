#!/usr/bin/env python
# -*- coding: utf-8 -*-


from argolib import ArgoDriver


def main():

  ### classe = anno_sezione
  classe = '{{ scrutinio.classe.anno }}{{ scrutinio.classe.sezione }}'

  ### esegue programma
  ad = ArgoDriver(debug="SCREEN", logpath="{{ percorso }}", logfile="{{ scrutinio.classe.anno }}{{ scrutinio.classe.sezione }}-blocco-GS")
  ad.bloccaFinale(classe)


if __name__ == "__main__":
  main()

