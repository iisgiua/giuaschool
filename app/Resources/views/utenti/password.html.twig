{% extends 'utenti/index.html.twig' %}

{% block pagina_contenuto %}
<div class="container-fluid">
  {% if errore %}
  <div class="alert alert-danger gs-mt-5">
    <p class="gs-big"><strong>{{ errore|trans|raw }}</strong></p>
  </div>
  {% else %}
    {% include('include/form.html.twig') %}
  {% endif %}
</div>
{% endblock %}
